# A-Maze-ing - default configuration file
# Lines starting with # are comments and are ignored.
# Format: KEY=VALUE (one per line, no spaces around the =)

WIDTH=10
HEIGHT=10
#ENTRY=8,9
EXIT=0,0
OUTPUT_FILE=maze.txt
PERFECT=false

# Uncomment the line below to always get the same maze (reproducible).
#SEED=42

#ALGORITHM=wilson
