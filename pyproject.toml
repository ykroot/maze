[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A maze generation and solving module"
authors = [{name = "Student B"}]
requires-python = ">=3.10"
